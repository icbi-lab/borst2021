params {
  publishDirMode = "copy"
  resultsDir = "results"
  deployDir = "deploy"
}

process {
  cache = 'deep'
  cpus = 1
  errorStrategy = 'finish'
  containerOptions = "--no-home"
}


resume = true

singularity.enabled = true
singularity.autoMounts = true
